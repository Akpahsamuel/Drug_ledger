{
  "name": "drugledger-contracts",
  "version": "1.0.0",
  "description": "DrugLedger smart contracts for the Sui blockchain",
  "scripts": {
    "dev": "sui move build",
    "build": "sui move build",
    "test": "sui move test",
    "publish": "sui client publish --gas-budget 100000000"
  },
  "dependencies": {
    "@mysten/sui": "^1.27.0"
  },
  "devDependencies": {
    "@mysten/sui": "^1.27.0"
  }
} 